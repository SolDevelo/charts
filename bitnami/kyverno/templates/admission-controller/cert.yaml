{{- /*
Copyright Broadcom, Inc. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/}}

{{- if and (not .Values.admissionController.tls.existingSecret) .Values.kyverno.tls.autoGenerated.enabled (eq .Values.kyverno.tls.autoGenerated.engine "cert-manager") }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ printf "%s-crt" (include "kyverno.admission-controller.fullname" .) }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" (dict "customLabels" .Values.commonLabels "context" .) | nindent 4 }}
    app.kubernetes.io/component: admission-controller
    app.kubernetes.io/part-of: kyverno
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" (dict "value" .Values.commonAnnotations "context" .) | nindent 4 }}
  {{- end }}
spec:
  secretName: {{ template "kyverno.admission-controller.tlsSecretName" . }}
  commonName: {{ printf "%s.%s.svc.%s" (include "kyverno.admission-controller.fullname" .) (include "common.names.namespace" .) .Values.clusterDomain }}
  issuerRef:
    name: {{ printf "%s-ca-issuer" (include "common.names.fullname" .) }}
    kind: Issuer
  subject:
    organizations:
      - "kyverno"
  dnsNames:
  {{- $dnsNames := include "common.certs.sans" (dict "namespace" (include "common.names.namespace" .) "clusterDomain" .Values.clusterDomain "serviceName" (include "kyverno.admission-controller.fullname" .) "loopback" .Values.kyverno.tls.autoGenerated.loopback "extraSANs" (.Values.kyverno.tls.autoGenerated.extraSANs | default (list))) | splitList " " | uniq }}
    {{- range $dnsNames }}
    - {{ . | quote }}
    {{- end }}
  {{- if .Values.kyverno.tls.autoGenerated.loopback }}
  ipAddresses:
    - 127.0.0.1
  {{- end }}
  privateKey:
    algorithm: {{ .Values.kyverno.tls.autoGenerated.certManager.keyAlgorithm }}
    size: {{ int .Values.kyverno.tls.autoGenerated.certManager.keySize }}
  duration: {{ .Values.kyverno.tls.autoGenerated.certManager.duration }}
  renewBefore: {{ .Values.kyverno.tls.autoGenerated.certManager.renewBefore }}
{{- end }}
