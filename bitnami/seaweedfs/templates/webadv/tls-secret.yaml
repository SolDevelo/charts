{{- /*
Copyright Broadcom, Inc. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/}}

{{- if and .Values.filer.enabled .Values.webdav.enabled .Values.webdav.tls.enabled (not .Values.webdav.tls.existingSecret) (or (not .Values.webdav.tls.autoGenerated.enabled) (and .Values.webdav.tls.autoGenerated.enabled (eq .Values.webdav.tls.autoGenerated.engine "helm"))) }}
{{- $secretName := include "seaweedfs.webdav.tls.secretName" . }}
{{- $caValue := "" }}
{{- $certValue := "" }}
{{- $keyValue := "" }}
{{- if .Values.webdav.tls.autoGenerated.enabled }}
  {{- $ca := genCA "webdav-ca" 365 }}
  {{- $serviceName := include "seaweedfs.webdav.fullname" . }}
  {{- $ipAddresses := ternary (list "127.0.0.1") nil .Values.webdav.tls.autoGenerated.loopback }}
  {{- $sans := include "common.certs.sans" (dict "namespace" (include "common.names.namespace" .) "clusterDomain" .Values.clusterDomain "serviceName" $serviceName "loopback" .Values.webdav.tls.autoGenerated.loopback "extraSANs" (.Values.webdav.tls.autoGenerated.extraSANs | default (list))) | splitList " " | uniq }}
  {{- $cert := genSignedCert $serviceName $ipAddresses $sans 365 $ca }}
  {{- $caValue = $ca.Cert }}
  {{- $certValue = $cert.Cert }}
  {{- $keyValue = $cert.Key }}
{{- else }}
  {{- $caValue = .Values.webdav.tls.ca }}
  {{- $certValue = required "When webdav.tls.autoGenerated.enabled=false you need to provide webdav.tls.cert" .Values.webdav.tls.cert }}
  {{- $keyValue = required "When webdav.tls.autoGenerated.enabled=false you need to provide webdav.tls.key" .Values.webdav.tls.key }}
{{- end }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: webdav
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
type: kubernetes.io/tls
data:
  tls.crt: {{ include "common.secrets.lookup" (dict "secret" $secretName "key" "tls.crt" "defaultValue" $certValue "context" .) }}
  tls.key: {{ include "common.secrets.lookup" (dict "secret" $secretName "key" "tls.key" "defaultValue" $keyValue "context" .) }}
  {{- if $caValue }}
  ca.crt: {{ include "common.secrets.lookup" (dict "secret" $secretName "key" "ca.crt" "defaultValue" $caValue "context" .) }}
  {{- end }}
{{- end }}
