{{- /*
Copyright Broadcom, Inc. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/}}

{{- if and .Values.tls.enabled (not .Values.tls.existingSecret) (or (not .Values.tls.autoGenerated.enabled) (and .Values.tls.autoGenerated.enabled (eq .Values.tls.autoGenerated.engine "helm"))) }}
{{- $secretName := include "nifi.tls.secretName" . }}
{{- $caValue := "" }}
{{- $certValue := "" }}
{{- $keyValue := "" }}
{{- if .Values.tls.autoGenerated.enabled }}
  {{- $ca := genCA "nifi-ca" 365 }}
  {{- $fullname := include "common.names.fullname" . }}
  {{- $serviceName := include "common.names.fullname" . }}
  {{- $headlessServiceName := include "nifi.headlessServiceName" . }}
  {{- $ipAddresses := ternary (list "127.0.0.1") nil .Values.tls.autoGenerated.loopback }}
  {{- $sans := include "common.certs.sans" (dict "namespace" (include "common.names.namespace" .) "clusterDomain" .Values.clusterDomain "serviceName" $serviceName "headlessServiceName" $headlessServiceName "loopback" .Values.tls.autoGenerated.loopback "extraSANs" (concat .Values.tls.autoGenerated.extraSANs (splitList "," (include "nifi.web.proxyHosts" .)) | default (list))) | splitList " " | uniq }}
  {{- $cert := genSignedCert $serviceName $ipAddresses $sans 365 $ca }}
  {{- $caValue = $ca.Cert }}
  {{- $certValue = $cert.Cert }}
  {{- $keyValue = $cert.Key }}
{{- else }}
  {{- $caValue = .Values.tls.ca }}
  {{- $certValue = required "When tls.autoGenerated.enabled=false you need to provide tls.cert" .Values.tls.cert }}
  {{- $keyValue = required "When tls.autoGenerated.enabled=false you need to provide tls.key" .Values.tls.key }}
{{- end }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/part-of: nifi
    app.kubernetes.io/component: nifi
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
type: kubernetes.io/tls
data:
  tls.crt: {{ include "common.secrets.lookup" (dict "secret" $secretName "key" "tls.crt" "defaultValue" $certValue "context" .) }}
  tls.key: {{ include "common.secrets.lookup" (dict "secret" $secretName "key" "tls.key" "defaultValue" $keyValue "context" .) }}
  {{- if $caValue }}
  ca.crt: {{ include "common.secrets.lookup" (dict "secret" $secretName "key" "tls.ca" "defaultValue" $caValue "context" .) }}
  {{- end }}
{{- end }}
